svg.append("text")
.attr("class", "x label")
.attr("text-anchor", "end")
.attr("x", width/2 + padding/2)
.attr("y", height - 6)
.text("Minute of game");

svg.append("text")
.attr("class", "y label")
.attr("text-anchor", "end")
.attr("y", 5)
.attr("x", -height/2 - padding/2)
.attr("dy", ".65em")
.attr("transform", "rotate(-90)")
.text("FG%");
